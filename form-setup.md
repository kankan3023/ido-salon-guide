# クイズ選手権 集計フォームの設定手順（幹事用）

参加者が現地クイズ20問に全問回答すると、ホーム画面に「🏆 クイズ選手権にエントリー」カードが出ます。
名前と「ニアピン問題」の予想を入れて「結果を送信する」を押すと、成績が事前入力されたGoogleフォームが開き、参加者は「送信」を押すだけです。
回答はスプレッドシートに自動で溜まり、関数でランキングになります。

**順位の決め方**: 正解数（多い順）→ 同点はニアピン問題（予想が正解に近い順）→ それも同じなら20問目の回答完了時刻（早い順）。

**ニアピン問題**: 「今日の参加者全員の正解数を合計すると何点？」。正解は集計シートの合計で機械的に出ます。正解発表は納涼会でどうぞ。

## 1. Googleフォームを作る

[Googleフォーム](https://docs.google.com/forms) で新規フォームを作成し、**この順番で** 記述式（短文回答）の質問を6つ作ります。

1. 名前
2. スコア
3. ニアピン予想
4. 完了時刻
5. 回答パターン
6. チェックコード

- すべて「記述式」でOK。必須にしてください。
- 設定 →「メールアドレスを収集する」は **オフ**（参加者の手間を減らすため）
- 「回答を1回に制限する」も **オフ**（Googleログインが必要になってしまうため）

## 2. entry ID を調べて index.html に貼る

1. フォーム右上「︙」→「事前入力したリンクを取得」
2. 6つの質問に適当な目印（例: 名前欄に `AAA`、スコア欄に `BBB` …）を入れて「リンクを取得」
3. 出てきたURLの中の `entry.数字=AAA` のような部分を見て、どの数字がどの質問か控える

`index.html` の `var FORM = {` の部分を書き換えます:

```js
var FORM = {
  url: "https://docs.google.com/forms/d/e/XXXXXXXX/viewform",  // ←フォームのURL（/viewform まで）
  entry: {
    name:     "entry.111111",  // 名前
    score:    "entry.222222",  // スコア
    guess:    "entry.333333",  // ニアピン予想
    finished: "entry.444444",  // 完了時刻
    pattern:  "entry.555555",  // 回答パターン
    code:     "entry.666666"   // チェックコード
  }
};
```

書き換えたら再デプロイ（URLは変わりません）。

## 3. スプレッドシートでランキング

フォームの「回答」タブ →「スプレッドシートにリンク」でシートを作成。
回答シートの列は A:タイムスタンプ / B:名前 / C:スコア / D:ニアピン予想 / E:完了時刻 / F:回答パターン / G:チェックコード になります。

新しいシート「ランキング」を作ります。

**B1 にニアピンの正解（全員の正解数合計）:**

```
=SUM(ARRAYFORMULA(IFERROR(VALUE(LEFT('フォームの回答 1'!C2:C, FIND("/", 'フォームの回答 1'!C2:C)-1)))))
```

**A3 にランキング本体:**

```
=SORT(
  {'フォームの回答 1'!B2:B,
   ARRAYFORMULA(IF('フォームの回答 1'!C2:C="",, VALUE(LEFT('フォームの回答 1'!C2:C, FIND("/", 'フォームの回答 1'!C2:C)-1)))),
   ARRAYFORMULA(IF('フォームの回答 1'!D2:D="",, ABS(VALUE('フォームの回答 1'!D2:D)-B1))),
   ARRAYFORMULA(IF('フォームの回答 1'!E2:E="",, VALUE(REGEXEXTRACT('フォームの回答 1'!E2:E, "\((\d+)\)"))))},
  2, FALSE, 3, TRUE, 4, TRUE)
```

→ 左から「名前・正解数・ニアピン差・完了時刻(数値)」が、**正解数の多い順 → ニアピン差の小さい順 → 完了が早い順** に並びます。
同じ人が複数回送信していたら、一番上の行だけ採用してください。

※ 注意: 同じ名前の人が複数回送信すると B1 の合計（＝ニアピン正解）が水増しされます。表彰前に重複行を削除してから読み上げてください。

## 4. チェックコードの検算（任意）

スコアの手入力改ざんを見抜くためのコードです。スプレッドシートの
「拡張機能 → Apps Script」に以下を貼って保存:

```js
function CHECKCODE(name, pattern, guess, finishedField) {
  var m = String(finishedField).match(/\((\d+)\)/);
  var finished = m ? m[1] : "";
  var src = name + "|" + pattern + "|" + guess + "|" + finished + "|ido-salon-2026-tsukuba";
  var h = 5381;
  for (var i = 0; i < src.length; i++) h = ((h * 33) ^ src.charCodeAt(i)) >>> 0;
  return ("00000" + h.toString(36).toUpperCase()).slice(-6);
}
```

回答シートの空き列（H2など）に 1行ずつ:

```
=IF(CHECKCODE(B2, F2, D2, E2)=G2, "OK", "❌不一致")
```

「❌不一致」の行は手入力で書き換えられた可能性があります。

また、スコア欄と回答パターン欄の食い違い（例: スコア20/20なのにパターンに正解でない選択肢が多い）も目視の手がかりになります。

## 当日の流れ（目安）

| 時刻 | やること |
|---|---|
| 〜16:45 | 参加者がJAXA見学を終え、20問回答完了 |
| 17:00〜 | 帰りのバスで各自エントリー送信（圏外なら電波が入り次第） |
| 18:30頃 | 幹事がランキングシートを確認、上位3名を確定 |
| 19:00〜 | 納涼会で表彰🏆 ニアピン正解（B1の数字）の発表もお忘れなく |
