# クイズ選手権 集計サーバー(GAS)の設定手順（幹事用）

エントリー送信は、アプリから幹事のスプレッドシートへ**直接送信**される方式になりました。
参加者は「結果を送信する」を押すだけ。フォーム画面は出ません。
チェックコードの照合とスコアの検算はサーバー側で自動実行され、あやしい送信には ⚠️ が付きます。
うまく届かない場合だけ、予備として従来のGoogleフォームが自動で開きます（作成済みのフォームはそのまま残してください）。

**順位の決め方**: 正解数（多い順）→ 同点はニアピン（予想が正解に近い順）→ それも同じなら完了時刻（早い順）。「ランキング」シートに自動で並びます。

## あなたがやること（10分・4ステップ）

### 1. スプレッドシートを新規作成

1. ブラウザで https://sheets.new を開く（Googleアカウントでログイン）
2. 左上のタイトルを「移動サロン2026 クイズ集計」などに変更

### 2. Apps Script にコードを貼る

1. メニューの **拡張機能 → Apps Script** をクリック（新しいタブが開く）
2. エディタに最初から入っている `function myFunction() {...}` を**全部消す**
3. リポジトリの **`gas-code.gs`** の中身を全部コピーして貼り付け
4. Ctrl+S（保存）。プロジェクト名は「クイズ集計」などに

### 3. 初期化を1回実行

1. エディタ上部の関数選択プルダウンで **`setup`** を選び、**「実行」**をクリック
2. 初回は権限の承認画面が出ます:
   「権限を確認」→ 自分のアカウントを選択 →「詳細」→「（プロジェクト名）に移動（安全ではないページ）」→「許可」
   ※自分で作ったスクリプトに自分が許可するだけなので安全です
3. 実行が終わると、スプレッドシートに「エントリー」「ランキング」の2シートができています

### 4. ウェブアプリとしてデプロイ

1. エディタ右上の **「デプロイ」→「新しいデプロイ」**
2. 左の歯車アイコン → 種類で **「ウェブアプリ」** を選択
3. 設定を次の通りに:
   - 説明: 任意（例: v1）
   - **次のユーザーとして実行: 自分**
   - **アクセスできるユーザー: 全員**（←ここ重要。「全員」でないと参加者のスマホから送信できません）
4. 「デプロイ」→ 表示された **ウェブアプリのURL**（`https://script.google.com/macros/s/…/exec`）をコピー
5. **そのURLをClaudeに渡してください。** アプリへの埋め込みと疎通テストはこちらでやります

動作確認したいときは、コピーしたURLをブラウザで開いてみてください。`{"ok":true,...}` と表示されれば生きています。

## 当日の見方

- **「ランキング」シート**: 上から順位どおりに自動で並びます。B1セルがニアピンの正解（全員の正解数合計）——納涼会での発表をお忘れなく
- **「エントリー」シート**: 受信した生データ。右端の「検証」列が **OK** なら正常。**⚠️** が付いた行は改ざんの疑いがあるので、表彰前に本人に確認を
- 同じ人が複数回送信した場合は**最初の行だけ採用**し、重複行は削除してください（ニアピン正解の合計が水増しされるため）
- 予備フォールバックのGoogleフォームに回答が入った場合は、フォームの回答画面から手動で「エントリー」シートに転記してください（圏外時の保険なので、通常は0件のはずです）

## コードを直したとき（再デプロイ）

「デプロイ」→「デプロイを管理」→ 鉛筆アイコン → バージョン:「新バージョン」→「デプロイ」。
**URLは変わりません**（「新しいデプロイ」を作るとURLが変わってしまうので注意）。
